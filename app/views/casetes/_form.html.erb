<%= form_with(model: casete, local: true) do |form| %>
  <% if casete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(casete.errors.count, "error") %> prohibited this casete from being saved:</h2>

      <ul>
      <% casete.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :formato %>
    <%= form.text_field :formato %>
  </div>

  <div class="field">
    <%= form.label :pelicula_id %>
    <%= form.text_field :pelicula_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
